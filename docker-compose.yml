version: '3.8'

services:
  mi-backend:
    build: .
    container_name: contenedor-spring-app
    ports:
      - "8080:8080"
    environment:
      # Si en el futuro usas variables, van aquí
      - SERVER_PORT=8080
      - SPRING_PROFILES_ACTIVE=dev
    restart: always
    